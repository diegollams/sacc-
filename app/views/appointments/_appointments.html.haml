.text-center
.panel.panel-default
  .panel-heading
    %i.fa.fa-tasks.fa-fw
    = ((date - Date.today).days == 0 ? t('today') : date)
  .panel-body
    %ul.chat
      - appointments.each do |appointment|
        = render partial: 'appointment', locals: {appointment: appointment}
  .panel-footer